# Скопируй в .env рядом с docker-compose.yml: cp .env.example .env
# Пароль для входа в админку (обязателен)
ADMIN_PASSWORD=придумай-сильный-пароль
# Секрет для подписи сессий — любая длинная случайная строка (openssl rand -hex 32)
AUTH_SECRET=
